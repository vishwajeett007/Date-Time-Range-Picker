import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">Date-Time Range Picker</h1>
      <p className="text-gray-600">Open Storybook to see the component in action.</p>
    </div>
  </React.StrictMode>
);
